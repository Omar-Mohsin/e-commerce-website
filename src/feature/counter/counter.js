import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    count : 0,
}

const countSlice = createSlice({
name : counts , 
initialState , 
reducers : {

    incrementCounter :  {
        reducer (state , action) {
                
        }
    }
}

})